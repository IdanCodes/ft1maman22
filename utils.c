#include "utils.h"

/* max: get the greater number from the two given numbers */
double max(double a, double b) {
    return a > b ? a : b;
}

/* min: get the lower number from the two given numbers */
double min(double a, double b) {
    return a < b ? a : b;
}
